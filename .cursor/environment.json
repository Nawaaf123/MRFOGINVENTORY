{
  "name": "Stock Keeper",
  "install": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt\ncd frontend && npm install --legacy-peer-deps",
  "start": "sudo pg_ctlcluster 16 main start || true\nsudo -u postgres psql -c \"ALTER USER postgres PASSWORD 'postgres';\" || true\nsudo -u postgres psql -tc \"SELECT 1 FROM pg_database WHERE datname='stockkeeper'\" | grep -q 1 || sudo -u postgres createdb -O postgres stockkeeper\ncd backend && .venv/bin/python init_db.py",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && source .venv/bin/activate && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
      "description": "FastAPI backend on port 8000"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0",
      "description": "Vite frontend on port 5179"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 5179 },
    { "name": "backend", "port": 8000 }
  ]
}
